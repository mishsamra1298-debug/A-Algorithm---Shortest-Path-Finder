# A-Algorithm---Shortest-Path-Finder
This project demonstrates the A* algorithm to find the shortest path in a grid by combining cost and heuristic functions. It allows users to visualize path finding in different scenarios.+s
